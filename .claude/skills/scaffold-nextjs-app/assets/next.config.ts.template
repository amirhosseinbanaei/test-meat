import type { NextConfig } from 'next';

/**
 * Locked Next.js 16 configuration for the FE team.
 *
 * See references/locked-stack.md for the full list of packages this scaffold installs
 * and the FE Lead's rule for each.
 */
const nextConfig: NextConfig = {
  // React Compiler — automatic memoisation. See references/react-compiler-setup.md.
  reactCompiler: true,

  // Typed routes — type-checked <Link href> targets across the whole app.
  typedRoutes: true,

  // Strict mode (default in 16, set explicitly to make the contract visible).
  reactStrictMode: true,

  // Image hosts must be allow-listed. Never set `unoptimized: true`.
  images: {
    remotePatterns: [
      // Add an entry per external image host:
      // {
      //   protocol: 'https',
      //   hostname: 'images.example.com',
      // },
    ],
  },

  // Forward any non-public env vars the backend client needs through proxy.ts,
  // never through `env` here (would leak to client bundle).
};

export default nextConfig;
