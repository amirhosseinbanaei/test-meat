// ESLint flat config for the FE team.
//
// Rules:
// - We rely on `eslint-plugin-react-hooks@latest` — it now ships the React Compiler
//   rules as part of its recommended preset. Do NOT install `eslint-plugin-react-compiler`
//   alongside it; that package is deprecated.
// - Next.js core-web-vitals brings the framework-specific rules.
// - TypeScript checking happens through `tsc --noEmit` in the lint-and-typecheck skill,
//   not duplicated here.

import { FlatCompat } from '@eslint/eslintrc';
import { dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

const compat = new FlatCompat({
  baseDirectory: __dirname,
});

const eslintConfig = [
  // Next.js + React + React Hooks (with compiler rules) + a11y.
  ...compat.extends(
    'next/core-web-vitals',
    'next/typescript',
    'plugin:react-hooks/recommended-latest'
  ),

  {
    rules: {
      // FE team additions go here. Keep minimal — prefer compiler/type checks over style rules.
      // Example:
      // 'no-console': ['warn', { allow: ['warn', 'error'] }],
    },
  },
];

export default eslintConfig;
