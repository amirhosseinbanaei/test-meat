import { Skeleton } from '@/common/components/ui/skeleton';

/**
 * Shown while the 3D scene chunks and the model are loading.
 * Replace with a more on-brand placeholder if the design requires it.
 *
 * Keep it the same dimensions as the final Canvas to prevent layout shift.
 */

export function SceneFallback() {
  return (
    <div className="flex h-full w-full items-center justify-center">
      <div className="flex flex-col items-center gap-3">
        <Skeleton className="size-32 rounded-full" />
        <span className="text-sm text-muted-foreground">Loading 3D scene…</span>
      </div>
    </div>
  );
}
